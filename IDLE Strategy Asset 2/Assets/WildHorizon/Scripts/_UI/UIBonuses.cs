using TMPro;
using UnityEngine;

namespace IdleStrategyKit
{
    public class UIBonuses : MonoBehaviour
    {
        public GameObject slotPray;
        public TextMeshProUGUI textInfo;


        //public Transform content;

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {
            /*slotPray.SetActive(Global.prayType != PreyType.none);
            if (Global.prayType == PreyType.buildSpeed) textInfo.text = "30% \n Build";
            else if (Global.prayType == PreyType.researchSpeed) textInfo.text = "30% \nResearch";*/
        }
    }
}


